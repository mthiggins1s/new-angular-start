import { Component } from "@angular/core";

@Component({
    selector: 'app-header',
    templateUrl: './header.component.html',
    styleUrl: './header.component.css',
    standalone: true, // marks component as a standalone component.
    imports: [],
})
export class HeaderComponent {} // HeaderComponent class; a class that will define a component for the header.